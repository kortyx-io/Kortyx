# Enable workspaces
workspaces=true

# Prevent phantom dependencies
prefer-workspace-packages=true

# Faster installs
prefer-offline=true

# Force consistent lockfile
package-lock=true

# Disable workspaces experimental
workspaces-experimental=false
